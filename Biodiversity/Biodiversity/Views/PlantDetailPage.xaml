<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Biodiversity.Views.PlantDetailPage">
    <ContentPage.Content>
        <ScrollView>
<<<<<<< HEAD
=======
            <!--
            <StackLayout Spacing="20" Padding="15">
                <Frame BorderColor="#2196F3" CornerRadius="5" Padding="8">
                    <StackLayout>
                        <Image Source="{Binding ImagePath}"/>
                        <BoxView Color="Gray"
                            HeightRequest="2"
                            HorizontalOptions="Fill" />
                        <Label Text="{Binding Name}" FontSize="Medium"/>
                    </StackLayout>
                </Frame>
                <Label Text="General Information" FontSize="Medium" />
                <Label Text="{Binding Desc}" FontSize="Small"/>
                <Button Text="View Location"             
                HorizontalOptions="Center"
                Clicked="OnLocationClicked" />
            </StackLayout>
            -->
>>>>>>> 309fbfe573668d8804bcd10861e80614bbe197d0
            <Grid RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

<<<<<<< HEAD
                <BoxView Grid.Row="0" Color="#299479"/>
                <StackLayout Grid.Row="0" Spacing="10" Padding="15" VerticalOptions="CenterAndExpand">
                    <Image Source="{Binding ImagePath}"/>
                    <Label Text="{Binding Name}" FontSize="Large" TextColor="White" HorizontalOptions="CenterAndExpand"/>
                </StackLayout>
                <BoxView Grid.Row="1" Color="#1b7f7d"/>
                <StackLayout Grid.Row="1" Spacing="20" Padding="15">
                    <Label Text="General Information" FontSize="Large" TextColor="White"/>
                    <Label Text="{Binding Desc}" FontSize="Medium" TextColor="White"/>
=======
                <BoxView Grid.Row="0" Color="#19F806"/>
                <StackLayout Grid.Row="0" Spacing="10" Padding="15" VerticalOptions="CenterAndExpand">
                    <Image Source="{Binding ImagePath}"/>
                    <Label Text="{Binding Name}" FontSize="Medium" TextColor="#000000" HorizontalOptions="CenterAndExpand"/>
                </StackLayout>
                <BoxView Grid.Row="1" Color="#11571C"/>
                <StackLayout Grid.Row="1" Spacing="20" Padding="15">
                    <Label Text="General Information" FontSize="Medium" TextColor="White"/>
                    <Label Text="{Binding Desc}" FontSize="Small" TextColor="White"/>
>>>>>>> 309fbfe573668d8804bcd10861e80614bbe197d0
                    <Button Text="View Location"             
                        HorizontalOptions="Center"
                        Clicked="OnLocationClicked" />
                </StackLayout>
            </Grid>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>